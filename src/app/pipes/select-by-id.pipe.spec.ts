import { SelectByIdPipe } from './select-by-id.pipe';

describe('SelectByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
